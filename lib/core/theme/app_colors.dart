import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Neubrutalism palette - bold, flat, high contrast
  static const primary = Color(0xFFFFD700); // Yellow neon
  static const primaryDark = Color(0xFFFFB300);
  static const secondary = Color(0xFF00E5FF); // Cyan
  static const accent = Color(0xFFFF4081); // Pink
  static const success = Color(0xFF00E676); // Green
  static const error = Color(0xFFFF1744); // Red
  static const warning = Color(0xFFFF9100); // Orange

  // Neutrals
  static const background = Color(0xFFF5F5F5);
  static const surface = Color(0xFFFFFFFF);
  static const card = Color(0xFFFFFFFF);
  static const black = Color(0xFF1A1A1A);
  static const blackSoft = Color(0xFF2D2D2D);
  static const greyDark = Color(0xFF666666);
  static const grey = Color(0xFF999999);
  static const greyLight = Color(0xFFCCCCCC);
  static const border = Color(0xFF1A1A1A); // Black border for neubrutalism
  static const borderLight = Color(0xFF333333);

  // Gen Z vibes
  static const lime = Color(0xFFB2FF59);
  static const purple = Color(0xFFD500F9);
  static const coral = Color(0xFFFF6D6D);

  // Dark mode
  static const darkBackground = Color(0xFF121212);
  static const darkSurface = Color(0xFF1E1E1E);
  static const darkCard = Color(0xFF252525);
}
